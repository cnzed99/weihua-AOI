<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:attach="clr-namespace:WH.Controls.Controls.Attach"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:wh="clr-namespace:WH.Controls">
    <Style x:Key="StatusDotLabel" TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="{DynamicResource RegionBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="hc:BorderElement.CornerRadius" Value="{StaticResource DefaultCornerRadius}" />
        <Setter Property="Padding" Value="{StaticResource DefaultControlPadding}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Height" Value="{StaticResource DefaultControlHeight}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Label">
                    <hc:SimplePanel>
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{Binding Path=(hc:BorderElement.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                            SnapsToDevicePixels="true">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                        <wh:StatusLight
                            Margin="5"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Fill="{Binding Path=(attach:StatusDotElement.DotBrush), RelativeSource={RelativeSource TemplatedParent}}"
                            Radius="{Binding Path=(attach:StatusDotElement.Radius), RelativeSource={RelativeSource TemplatedParent}}" />
                    </hc:SimplePanel>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style
        x:Key="StatusDotLabel.Small"
        BasedOn="{StaticResource StatusDotLabel}"
        TargetType="Label">
        <Setter Property="Height" Value="20" />
        <Setter Property="Padding" Value="6,0" />
    </Style>
    <Style
        x:Key="StatusDotLabelPrimary"
        BasedOn="{StaticResource StatusDotLabel}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
    </Style>

    <Style
        x:Key="StatusDotLabelPrimary.Small"
        BasedOn="{StaticResource StatusDotLabelPrimary}"
        TargetType="Label">
        <Setter Property="Height" Value="20" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="StatusDotLabelSuccess"
        BasedOn="{StaticResource StatusDotLabel}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource SuccessBrush}" />
    </Style>

    <Style
        x:Key="StatusDotLabelSuccess.Small"
        BasedOn="{StaticResource StatusDotLabelSuccess}"
        TargetType="Label">
        <Setter Property="Height" Value="20" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="StatusDotLabelInfo"
        BasedOn="{StaticResource StatusDotLabel}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource InfoBrush}" />
    </Style>

    <Style
        x:Key="StatusDotLabelInfo.Small"
        BasedOn="{StaticResource StatusDotLabelInfo}"
        TargetType="Label">
        <Setter Property="Height" Value="20" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="StatusDotLabelWarning"
        BasedOn="{StaticResource StatusDotLabel}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource WarningBrush}" />
    </Style>

    <Style
        x:Key="StatusDotLabelWarning.Small"
        BasedOn="{StaticResource StatusDotLabelWarning}"
        TargetType="Label">
        <Setter Property="Height" Value="20" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="StatusDotLabelDanger"
        BasedOn="{StaticResource StatusDotLabel}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource DangerBrush}" />
    </Style>

    <Style
        x:Key="StatusDotLabelDanger.Small"
        BasedOn="{StaticResource StatusDotLabelDanger}"
        TargetType="Label">
        <Setter Property="Height" Value="20" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

</ResourceDictionary>