<UserControl
    x:Class="WH.Controls.ImageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:WH.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    DataContext="{Binding Self}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="3">
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <DockPanel LastChildFill="False">
            <Button
                Name="btnClear"
                Width="30"
                Margin="5,0"
                Background="{x:Null}"
                Click="btnClear_Click"
                DockPanel.Dock="Right"
                Style="{StaticResource ButtonCustom}">
                <Image Source="/ImageWinCtrl;component/Images/清理.png" Stretch="Uniform" />
            </Button>
            <TextBlock
                Margin="5,0"
                VerticalAlignment="Center"
                DockPanel.Dock="Right">
                <TextBlock.Text>
                    <MultiBinding StringFormat="R:{0} G:{1} B:{2}">
                        <Binding ElementName="Image" Path="R" />
                        <Binding ElementName="Image" Path="G" />
                        <Binding ElementName="Image" Path="B" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </DockPanel>
        <DockPanel Grid.Row="2" LastChildFill="False">
            <TextBlock Margin="5,0" DockPanel.Dock="Right">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} | {1}">
                        <Binding ElementName="Image" Path="X" />
                        <Binding ElementName="Image" Path="Y" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </DockPanel>
        <Grid Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Disabled">
                <local:ImagePro x:Name="Image" />
            </ScrollViewer>
            <local:CanvasPro x:Name="Canvas" />
        </Grid>
    </Grid>
</UserControl>
