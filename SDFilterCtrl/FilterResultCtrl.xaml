<UserControl x:Class="SDFilter.FilterResultCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:local="clr-namespace:SDFilter"
             xmlns:entity="clr-namespace:WH.Entity.Converter;assembly=WH.Entity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="550">
    <d:UserControl.DataContext>
        <local:SDFilterCtrlVM/>
    </d:UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <!--<ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>-->
            <local:Result2Color x:Key="R2C"/>
            <entity:Enum2CmbConverter x:Key="Enum2CmbConverter"/>
            <local:TextUnitsConverter x:Key="TextUnitsConverter"/>
            <local:ValueHalfConverter x:Key="ValueHalfConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <hc:ScrollViewer VerticalScrollBarVisibility="Auto" IsInertiaEnabled="True">
            <ItemsControl ItemsSource="{Binding FilterConfig.SpeciesFilters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Header="{Binding Name}"  IsExpanded="True" Margin="0,0,0,1" BorderThickness="0" hc:BorderElement.CornerRadius="0" 
                                  Background="#FF043F3C" Foreground="{Binding Result,Converter={StaticResource R2C}}">
                            <ItemsControl ItemsSource="{Binding RecipeDefects}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel HorizontalAlignment="Left"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding DefectFilters}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel HorizontalAlignment="Left"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Expander BorderThickness="0" hc:BorderElement.CornerRadius="0" Background="Transparent" 
                                                              Foreground="{Binding RelativeSource={RelativeSource AncestorType=Expander}, Path=Foreground}"
                                                               Width="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ActualWidth, Converter={StaticResource ValueHalfConverter}, Mode=OneWay}">    
                                                        <Expander.Header>
                                                            <Grid Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Expander}}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="4*"/>
                                                                    <ColumnDefinition Width="1*"/>
                                                                    <ColumnDefinition Width="3*"/>
                                                                    <ColumnDefinition Width="30"/>
                                                                    <ColumnDefinition Width="3*"/>
                                                                    <ColumnDefinition Width="70"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{Binding Name}"/>
                                                                <TextBlock Grid.Column="2" Text="{Binding ResultList[0].Value}" TextAlignment="Center"/>
                                                                <TextBlock Grid.Column="3" Text="{Binding ResultList[0].Feature, Converter={StaticResource TextUnitsConverter}}" TextAlignment="Center"/>
                                                                <TextBlock Grid.Column="4"  Text="{Binding ResultList[1].Value}"  TextAlignment="Center"/>
                                                                <TextBlock Grid.Column="5" Text="{Binding ResultList[1].Feature, Converter={StaticResource TextUnitsConverter}}"  TextAlignment="Left"/>
                                                            </Grid>
                                                        </Expander.Header>
                                                        <Border Background="Transparent" BorderBrush="#FF0C424E" BorderThickness="1" Margin="4,2">
                                                            <ItemsControl ItemsSource="{Binding ResultList}" BorderThickness="0" hc:BorderElement.CornerRadius="0" Background="Transparent">
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <Grid>
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="1.1*"/>
                                                                                <ColumnDefinition Width="2*"/>
                                                                                <ColumnDefinition Width="1*"/>
                                                                            </Grid.ColumnDefinitions>
                                                                            <Label Grid.Column="0" Content="{Binding Feature, Converter={StaticResource Enum2CmbConverter}}" Margin="0,0" HorizontalContentAlignment="Center" Background="Transparent" BorderThickness="0"/>
                                                                            <Label Grid.Column="1" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Expander}, Path=Foreground}" Content="{Binding Value}" Margin="0,0" HorizontalContentAlignment="Center"  Background="Transparent" BorderThickness="0"/>
                                                                            <Label Grid.Column="2" Content="{Binding Feature, Converter={StaticResource TextUnitsConverter}}" Margin="0,0" HorizontalContentAlignment="Center" Background="Transparent" BorderThickness="0"/>
                                                                        </Grid>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </Border>
                                                    </Expander>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </hc:ScrollViewer>
    </Grid>
</UserControl>
