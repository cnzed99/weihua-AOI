<UserControl x:Class="ImageWinCtrl.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageWinCtrl"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml" />
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <d:UserControl.DataContext>
        <local:ImageViewViewModel/>
    </d:UserControl.DataContext>
    <Grid Margin="3">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <DockPanel LastChildFill="False">
            <Button DockPanel.Dock="Right" Margin="5,0" Background="{x:Null}" Width="30">
                <Image Source="/清理.png" Stretch="Uniform"/>
            </Button>
            <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" Margin="5,0">
                <TextBlock.Text>
                    <MultiBinding StringFormat="R:{0} G:{1} B:{2}">
                        <Binding ElementName="Image" Path="R"/>
                        <Binding ElementName="Image" Path="G"/>
                        <Binding ElementName="Image" Path="B"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </DockPanel>
        <DockPanel Grid.Row="2" LastChildFill="False">
            <TextBlock DockPanel.Dock="Right" Margin="5,0">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} | {1}">
                        <Binding ElementName="Image" Path="X"/>
                        <Binding ElementName="Image" Path="Y"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </DockPanel>
        <Grid Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Disabled" >
                <local:ImagePro x:Name="Image" BitmapSource="{Binding BitmapSource}" ImageOne="{Binding ImageOne, Mode=OneWayToSource}"/>
            </ScrollViewer>
            <local:CanvasPro CanvasOne="{Binding CanvasOne, Mode=OneWayToSource}"/>
        </Grid>
    </Grid>
</UserControl>
